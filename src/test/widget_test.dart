// I test unitari sono in db_helper_test.dart
// Questo file è mantenuto per compatibilità con il runner di default
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('runner placeholder — vedi db_helper_test.dart per test reali', () {
    expect(1 + 1, 2);
  });
}
